{
  "name": "phonebookbackend",
  "version": "0.0.1",
  "description": "exercise 3",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../submitFrontEnd/part2/phonebook && npm run build --prod && cp -r build ../../../submitBackEnd/",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m buildui && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "author": "Junsheng Tan",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.6"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.11.8",
    "morgan": "^1.10.0"
  }
}
